import { AppController } from "./controllers/AppController.js";

document.addEventListener('DOMContentLoaded', () => {
    const app = new AppController();
    console.log("✅ SkyBard Interface Loaded!");
});